# Streamlit UI
streamlit>=1.36,<1.39

# HF stack
transformers>=4.44,<4.47
accelerate>=0.33
safetensors>=0.4.3

# Torch (CPU wheel available for Py 3.13)
torch==2.5.1

# Retrieval + numerics (versions with 3.13 wheels)
scikit-learn>=1.5.2
numpy>=2.1.2
pandas>=2.2.2

# Optional but common dep that transformers may pull in
tokenizers>=0.19.1
